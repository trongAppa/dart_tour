void main() {
  var player = {'name': 'name', 'xp': 19.99, 'flag': false};

  // compile가 알아서 타입을 유추 함으로 var로도 변경가능
  // List가 들어가도 되고 List로 감싸도 됨
  Map<int, bool> flag = {1: true, 2: false, 3: false};
}
